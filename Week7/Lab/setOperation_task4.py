# TASK: Create the set shown below then use the mutator version of the union method, which
# is update() to add the two missing vowels to the set.
# vowels = set({"a", "e", "i"})


vowels = set({"a", "e", "i"})

vowels.update({"o", "u"})

print(vowels)
