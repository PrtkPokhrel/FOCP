# def findMax(a,b):
# """Finds the maximum of two values."""
# if ( a > b ):
# max = a
# else:
# max = b
# return max

# TASK: Input the above function definition. Once that is done make several calls to the
# function passing different argument values and displaying the returned value.


def findMax(a,b):
    """Finds the maximun of two values"""
    if (a>b):
        max=a
    else:
        max=b
    return max

print(findMax(3,4))
print(findMax(2,1))
print(findMax(5,9))


"""In the program various function calls are made with various value and displayed using print() function"""

